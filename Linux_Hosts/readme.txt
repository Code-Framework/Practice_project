Make changes to hosts file according to need
run the code using sudo as because the program require root access to cp the file to linux etc file-system
For backup ,this program will create a copy of orignal hosts as hosts.org in the /etc 
